# data/raw/*.csv is intentionally NOT ignored — shipping_rates.csv and
# port_congestion.csv are the real dataset this project runs on
# (~436KB total), not a placeholder. Commit them.
__pycache__/
*.pyc
.pytest_cache/
cargonex.db
*.db
frontend/node_modules/
frontend/dist/
frontend/.env
